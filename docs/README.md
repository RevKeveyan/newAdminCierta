# Документация API Cierta Admin

Добро пожаловать в документацию API для системы управления грузами Cierta Admin.

## 📚 Содержание документации

### 1. [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)
**Полная документация API** - детальное описание всех endpoints, форматов запросов и ответов.

**Содержит:**
- Архитектура базы данных (модели Load, Carrier, Customer)
- Полное описание всех API endpoints
- Форматы запросов и ответов
- Примеры использования
- Логика автоматического создания Carrier и Customer
- Обработка ошибок

**Для кого:** Backend разработчики, Frontend разработчики, QA инженеры

---

### 2. [FRONTEND_INTEGRATION_GUIDE.md](./FRONTEND_INTEGRATION_GUIDE.md)
**Руководство по интеграции фронтенда** - готовые компоненты и примеры кода для быстрой интеграции.

**Содержит:**
- Готовые React компоненты
- Утилиты для работы с API
- Обработка ошибок
- TypeScript типы
- Рекомендации по использованию

**Для кого:** Frontend разработчики

---

### 3. [SYSTEM_FLOW_DIAGRAMS.md](./SYSTEM_FLOW_DIAGRAMS.md)
**Схемы работы системы** - визуальные диаграммы процессов и потоков данных.

**Содержит:**
- Диаграммы создания Load
- Логика поиска и создания Carrier/Customer
- Структура базы данных
- Потоки данных
- Сценарии использования

**Для кого:** Все разработчики, архитекторы, аналитики

---

## 🚀 Быстрый старт

### Для Frontend разработчиков:

1. Начните с [FRONTEND_INTEGRATION_GUIDE.md](./FRONTEND_INTEGRATION_GUIDE.md)
2. Используйте готовые компоненты из руководства
3. Смотрите примеры в [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)

### Для Backend разработчиков:

1. Изучите [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)
2. Посмотрите схемы в [SYSTEM_FLOW_DIAGRAMS.md](./SYSTEM_FLOW_DIAGRAMS.md)
3. Проверьте Postman коллекцию: `Cierta_Admin_API.postman_collection.json`

---

## 🔑 Ключевые особенности

### Автоматическое создание Carrier и Customer

При создании Load система автоматически:
- ✅ Ищет существующих Carrier/Customer по уникальным полям
- ✅ Создает новые записи, если не найдены
- ✅ Сохраняет для повторного использования
- ✅ Обновляет существующие записи новыми данными

### Умный поиск

**Carrier** ищется по приоритету:
1. `mcNumber` (MC номер)
2. `dotNumber` (DOT номер)
3. Комбинация `name` + `companyName`
4. Только `name`

**Customer** ищется по:
- `companyName` (case-insensitive)

### Поддержка файлов

- Загрузка изображений и документов
- Автоматическая обработка через S3
- Поддержка множественной загрузки

### PDF генерация

- Автоматическая генерация BOL (Bill of Lading)
- Генерация Rate Confirmation
- Скачивание готовых PDF

---

## 📋 Основные API Endpoints

### Load (Грузы)
- `POST /loads` - Создание Load
- `GET /loads` - Список Loads (с пагинацией и фильтрацией)
- `GET /loads/:id` - Получение Load по ID
- `PUT /loads/:id` - Обновление Load
- `PUT /loads/:id/full` - Полное обновление с файлами
- `PUT /loads/:id/status` - Обновление статуса
- `DELETE /loads/:id` - Удаление Load
- `GET /loads/:id/bol` - Генерация BOL PDF
- `GET /loads/:id/history` - История изменений

### Carrier (Перевозчики)
- `POST /carriers` - Создание Carrier
- `GET /carriers` - Список Carriers
- `GET /carriers/:id` - Получение Carrier по ID
- `GET /carriers/search` - Поиск Carriers
- `GET /carriers/:id/loads` - Loads для Carrier

### Customer (Клиенты)
- `POST /customers` - Создание Customer
- `GET /customers` - Список Customers
- `GET /customers/:id` - Получение Customer по ID
- `GET /customers/search` - Поиск Customers
- `GET /customers/:id/loads` - Loads для Customer

---

## 📝 Примеры использования

### Создание Load с новым Carrier и Customer

```javascript
const formData = new FormData();

// Новый Customer
formData.append('customer', JSON.stringify({
  companyName: "ABC Company",
  customerAddress: {
    address: "123 Main St",
    city: "New York",
    state: "NY",
    zipCode: "10001"
  }
}));

// Новый Carrier
formData.append('carrier', JSON.stringify({
  name: "John Doe",
  mcNumber: "MC123456",
  phoneNumber: "+1122334455"
}));

// Остальные данные...
formData.append('type', JSON.stringify({ freight: true, vehicle: false }));
formData.append('pickup', JSON.stringify({ ... }));
formData.append('delivery', JSON.stringify({ ... }));

fetch('/loads', {
  method: 'POST',
  body: formData
});
```

### Использование существующего Carrier

```javascript
// Просто передайте ID
formData.append('carrier', JSON.stringify({
  id: "507f1f77bcf86cd799439012"
}));
```

---

## 🛠 Тестирование

### Postman

Используйте готовую коллекцию:
- `Cierta_Admin_API.postman_collection.json`
- `Cierta_Admin_API.postman_environment.json`

См. также: [postman-testing-guide.md](./postman-testing-guide.md)

---

## ⚠️ Важные замечания

1. **Формат данных:** При создании/обновлении Load используйте `multipart/form-data` для загрузки файлов
2. **JSON строки:** Вложенные объекты (customer, carrier, pickup, delivery) должны быть переданы как JSON строки в FormData
3. **Автоматическое создание:** Carrier и Customer создаются автоматически, если не найдены
4. **Уникальные поля:** `orderId` и `billOfLadingNumber` генерируются автоматически, если не указаны
5. **Статусы:** Доступные статусы Load: "Listed", "Dispatched", "Picked up", "Delivered", "On Hold", "Cancelled"

---

## 📞 Поддержка

При возникновении вопросов:
1. Проверьте документацию
2. Посмотрите примеры в Postman коллекции
3. Изучите схемы работы системы

---

## 📄 Дополнительные ресурсы

- [Postman Testing Guide](./postman-testing-guide.md)
- [System Architecture](../SYSTEM_ARCHITECTURE.md)
- [Command Reference](../COMMAND_REFERENCE.md)

---

**Версия документации:** 1.0  
**Последнее обновление:** 2024

